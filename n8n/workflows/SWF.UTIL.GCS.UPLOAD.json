{
    "name": "SWF.UTIL.GCS.UPLOAD",
    "nodes": [
        {
            "parameters": {},
            "name": "Start",
            "type": "n8n-nodes-base.executeWorkflowTrigger",
            "typeVersion": 1,
            "position": [
                250,
                300
            ]
        },
        {
            "parameters": {
                "authentication": "googleJsonKey",
                "bucketName": "={{$env.GCS_BUCKET_NAME}}",
                "fileName": "={{'Projects/' + $json.project_slug + '/' + $json.version + '/' + $json.path}}",
                "binaryData": false,
                "content": "={{$json.content}}"
            },
            "name": "Upload to GCS",
            "type": "n8n-nodes-base.googleCloudStorage",
            "typeVersion": 1,
            "position": [
                450,
                300
            ]
        }
    ],
    "connections": {
        "Start": {
            "main": [
                [
                    {
                        "node": "Upload to GCS",
                        "type": "main"
                    }
                ]
            ]
        }
    },
    "settings": {
        "errorWorkflow": "SWF.ERROR.CLASSIFY"
    }
}